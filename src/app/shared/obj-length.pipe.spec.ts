import { ObjLengthPipe } from './obj-length.pipe';

describe('ObjLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new ObjLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
